# 0.2.2 — plugin brand icons

The ComfyUI sidebar uses the phone-and-stars icon with theme-aware color. Manager's Registry card uses the existing green ComfyRemote brand image through the supported Icon metadata field.

No connector protocol, pairing format, execution or thumbnail behavior changes. Retains compatibility with both self-hosted and hosted services. Old release tags and attachments remain immutable.

Local: 33 Python tests and Ruff passed. Python 3.12/3.13 CI, fixed-version Manager installation, restart and actual preview image are verified separately before delivery. Registry review status is independent of installability.

The green star asset reproduces the existing Lucide Sparkles mark (ISC); attribution is included with the asset.
